import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        padding: 10,
        borderRadius: 5,
        margin: 10,
    },
    text: {
        color: 'white',
        fontSize: 16,
    },
});
export default styles;